<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GreenRoutes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="floatingActionButtonStyle">@style/FloatingActionButtonStyle</item>
    </style>

    <style name="Theme.GreenRoutes" parent="Base.Theme.GreenRoutes" />

    <!-- Style for FAB -->
    <style name="FloatingActionButtonStyle" parent="Widget.Design.FloatingActionButton">
        <item name="shapeAppearanceOverlay">@style/FloatingActionButtonStyle.Shape</item>
    </style>
    <style name="FloatingActionButtonStyle.Shape">
        <item name="cornerSize">25%</item>
        <item name="cornerFamily">rounded</item>
    </style>
</resources>